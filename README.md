# Overlays

To install:
```bash
mkdir -p $HOME/.config/nixpkgs    # Ensure ~/.config/nixpkgs exists

git \
  clone \
  git@github.com:mwilsoninsight/overlays.git \
  $HOME/.config/nixpkgs/overlays  # Clone the repo into ~/.config/nixpkgs
```
